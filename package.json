{
  "name": "smolmlive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Assistant vocal conversationnel 100% navigateur (LLM + ASR + VAD + TTS), statique, GitHub Pages ready",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "npx --yes serve -l 8080 ."
  }
}
